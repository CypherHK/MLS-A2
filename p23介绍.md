
# 项目中PART2-3总结：云端与边缘模型优化模拟


*   **Part 2: 云端优化 (Cloud-Side Optimization)**：专注于利用强大的云端计算资源，通过混合精度、分布式训练、梯度累积和知识蒸馏等技术，高效地训练出一个高性能的基准模型。
*   **Part 3: 边缘优化 (Edge-Side Optimization)**：专注于将模型变得更小、更快，以便部署在手机、嵌入式设备等资源受限的边缘环境中。此部分完整覆盖了神经架构搜索 (NAS)、模型剪枝 (Pruning) 和量化 (Quantization) 等关键技术。

---

# 模型架构分析
项目中主要使用了两种核心模型架构：一个标准的CNN模型和一个为效率优化的深度可分离卷积模型。

1. 标准CNN模型 (Standard CNN Model)
这是 Part 2 中所有云端优化技术的基础模型，同时也是 Part 3 模型剪枝流程中使用的“学生”模型。

定义脚本：
part2_cloud_rest_only.py 中的 build_cnn()
part3_edge_prune_only.py 中的 create_student_architecture()
内部构造：该模型基于CIFAR-10 数据集，含多个卷积、批归一化和池化层的 CNN 架构，该架构被有意设计得相对复杂和过参数化，以便为后续的优化技术（如剪枝、量化）提供充足的“优化空间”。其结构如下：
1.
输入层：接收 32x32x3 的图像。
2.
卷积块 (3个)：模型包含三个主要的卷积块，每个块的结构为：
Conv2D -> BatchNorm -> ReLU
Conv2D -> BatchNorm -> ReLU
MaxPooling2D
三个块的卷积核数量分别为 32, 64, 128。
3.
分类头：
GlobalAveragePooling2D：将特征图转换为向量。
Dropout(0.5)
Dense(256, activation="relu")
Dropout(0.3)
Dense(10)：输出 10 个类别的 logits。
2. 深度可分离卷积模型 (Depthwise Separable Conv Model)
这是 Part 3 中为边缘端专门设计和优化的模型，主要用于神经架构搜索 (NAS)、后续的独立训练和量化评估。

定义脚本：
part3_nas_only.py 中的 build_depthwise_student()
part3_train_depthwise_selected.py 中的 build_depthwise_student()
内部构造：该模型采用了类似 MobileNet 的高效结构，其核心是深度可分离卷积：
1.
输入层：接收 32x32x3 的图像。
2.
深度可分离卷积块 (3个)：模型包含三个主要的块，每个块的结构为：
DepthwiseConv2D (3x3 卷积核) -> BatchNorm -> ReLU
Conv2D (1x1 卷积核, Pointwise) -> BatchNorm -> ReLU
MaxPooling2D
这种结构将标准的卷积拆分为两步，大大减少了参数量和计算量。
3.
可变参数：模型的宽度 (width_mult) 和深度 (depth_mult) 是可变的，这正是神经架构搜索所要优化的目标。
4.
分类头：与标准CNN模型类似，但其全连接层的单元数和 Dropout 率有所不同。
架构使用总结
Part 2 和 Part 3 的模型是否一致？ 不完全一致。
Part 2 中使用的 标准CNN模型 被 Part 3 的剪枝流程继承并继续优化。
Part 3 的神经架构搜索、独立训练和量化流程则引入并使用了一个全新的、更高效的深度可分离卷积模型。
逻辑安排：项目首先通过知识蒸馏获得一个强大的标准CNN学生模型，然后兵分两路，一路通过剪枝优化这个学生模型，另一路则探索一个从设计上就更适合边缘部署的新架构。


## Part 2: 云端优化 (Cloud-Side Optimization)

**目标**：加速模型训练过程，并产出一个强大的、适合作为后续优化基础的“学生”模型。

### 1. 混合精度训练 (Mixed Precision)

*   **功能描述**：通过在训练中混合使用 `float16` 和 `float32` 精度，显著提升在兼容硬件上的训练速度并降低显存消耗。
*   **核心脚本**：<mcfile name="part2_cloud_mp_only.py" path="MLS-A2/part2_cloud_mp_only.py"></mcfile>
*   **实现函数**：在 `main` 函数中，通过 `mixed_precision.set_global_policy("mixed_float16")` 全局策略启用。
*   **基础模型**：通过 `build_cnn()` 函数构建的标准 CNN 模型。
*   **产出文件**：
    *   模型：`cloud_optimized_models/mp_cnn_ep15.keras`
    *   报告：`reports/cloud_mp.json`, `reports/history_mp.json`
*   **最终结果**：记录了混合精度训练下的训练时长和最终模型精度，验证了其加速效果。

### 2. 分布式训练 (Distributed Training)

*   **功能描述**：模拟数据并行训练，将训练任务分发到多个计算设备（本次仅仅模拟），以扩展训练规模、缩短训练时间。
*   **核心脚本**：<mcfile name="part2_cloud_rest_only.py" path="MLS-A2/part2_cloud_rest_only.py"></mcfile>
*   **实现函数**：在 `main` 函数中，通过 `tf.distribute.MirroredStrategy` 策略实现。
*   **基础模型**：通过 `build_cnn()` 函数构建的标准 CNN 模型。
*   **产出文件**：
    *   模型：`cloud_optimized_models/distributed_fp32_ep5.keras`
    *   报告：`reports/cloud_rest.json` (在 `distributed` 字段), `reports/history_distributed.json`
*   **最终结果**：记录了分布式训练下的总耗时、各周期耗时及模型评估结果。

### 3. 梯度累积 (Gradient Accumulation)

*   **功能描述**：在显存有限的情况下，通过累积多个小批次的梯度后进行单次权重更新，来模拟大批量（Large Batch Size）训练，有助于提升模型收敛的稳定性。
*   **核心脚本**：<mcfile name="part2_cloud_rest_only.py" path="MLS-A2/part2_cloud_rest_only.py"></mcfile>
*   **实现函数**：通过自定义的 `AccumModel` Keras 模型实现，其核心逻辑在 `train_step` 方法中。
*   **基础模型**：通过 `build_cnn()` 函数构建的标准 CNN 模型。
*   **产出文件**：
    *   模型：`cloud_optimized_models/batch_accum_fp32_ep10.keras`
    *   报告：`reports/cloud_rest.json` (在 `batch_processing` 字段), `reports/history_batch_accum.json`
*   **最终结果**：记录了梯度累积训练下的耗时与模型性能，验证了该方法在资源受限环境下的有效性。

### 4. 知识蒸馏 (Knowledge Distillation)

*   **功能描述**：训练一个参数量更大、结构更复杂的“教师”模型，然后利用其输出的“软标签”去指导一个轻量级的“学生”模型进行训练。这使得学生模型能以更小的体积达到接近教师模型的性能。
*   **核心脚本**：<mcfile name="part2_cloud_rest_only.py" path="MLS-A2/part2_cloud_rest_only.py"></mcfile>
*   **实现函数**：通过自定义的 `Distiller` Keras 模型实现，其核心逻辑在 `train_step` 方法中，结合了学生损失和蒸馏损失。
*   **基础模型**：
    *   教师模型：通过 `build_cnn(width_mult=1.6, ...)` 构建的更大模型。
    *   学生模型：通过 `build_cnn()` 构建的标准尺寸模型。
*   **产出文件**：
    *   模型：`cloud_optimized_models/kd_teacher_ep16.keras` (教师), `cloud_optimized_models/kd_student_ep16_alpha0_7_T2_0.keras` (学生) 及其 `.weights.h5` 权重文件。
    *   报告：`reports/cloud_rest.json` (在 `knowledge_distillation` 字段), `reports/history_kd_teacher.json`, `reports/history_kd_student.json`
*   **最终结果**：成功训练并产出了一个高性能、轻量化的学生模型，该模型是 **Part 3 边缘优化流程的起点**。

---

## Part 3: 边缘优化 (Edge-Side Optimization)

**目标**：将 Part 2 产出的学生模型或一个新搜索出的高效模型，通过架构搜索、剪枝和量化等手段，进一步优化为适合在边缘设备上部署的超轻量级模型。（只有剪枝采用的part2产出的学生模型，其他部分实际采用的NAS最优模型）

### 1. 神经架构搜索 (Neural Architecture Search, NAS)

*   **功能描述**：在一个预定义的搜索空间（模型宽度和深度）内，通过代理训练（Proxy Training）的方式，快速评估不同网络架构的性能，以自动发现最优或接近最优的模型结构。
*   **核心脚本**：<mcfile name="part3_nas_only.py" path="MLS-A2/part3_nas_only.py"></mcfile>
*   **实现函数**：在 `main` 函数的循环中，通过调用 `build_depthwise_student` 函数来构建和评估不同超参数的候选模型。
*   **基础模型**：基于 `build_depthwise_student` 函数动态生成的深度可分离卷积模型。
*   **产出文件**：
    *   报告：`reports/nas_search_results.json`
*   **最终结果**：一份包含多个候选架构及其代理评估准确率和参数量的报告，为选择最终模型提供了决策依据。

### 2. 训练选定的深度可分离模型

*   **功能描述**：根据 NAS 的结果或专家经验，选择一组最优的宽度和深度超参数，对深度可分离模型进行完整周期的训练，并保存其权重，作为后续剪枝和量化的基础。
*   **核心脚本**：<mcfile name="part3_train_depthwise_selected.py" path="MLS-A2/part3_train_depthwise_selected.py"></mcfile>
*   **实现函数**：`main` 函数接收超参数，调用 `build_depthwise_student` 构建模型并执行完整训练。
*   **基础模型**：通过 `build_depthwise_student` 函数构建的特定配置模型。
*   **产出文件**：
    *   模型：`edge_optimized_models/depthwise_w{...}_d{...}.keras` 及其 `.weights.h5` 权重文件。
    *   报告：`reports/train_depthwise_selected.json`
*   **最终结果**：一个训练充分、性能优良的深度可分离卷积模型，可直接用于量化，或作为剪枝的输入。

### 3. 模型剪枝 (Pruning)

*   **功能描述**：移除模型中数值较小的（冗余的）权重，以降低模型大小和计算复杂度。脚本采用 TensorFlow Model Optimization Toolkit (tfmot) 实现幅度剪枝，并在剪枝后对模型进行微调以恢复性能。
*   **核心脚本**：<mcfile name="part3_edge_prune_only.py" path="MLS-A2/part3_edge_prune_only.py"></mcfile>
*   **实现函数**：`prune_and_finetune` 函数使用 `tfmot.sparsity.keras.prune_low_magnitude` 对模型进行包装和训练。
*   **基础模型**：Part 2 中由知识蒸馏产出的学生模型 (`cloud_optimized_models/kd_student_ep16_alpha0_7_T2_0.weights.h5`)。
*   **产出文件**：
    *   模型：`edge_optimized_models/student_pruned_s{...}.keras` (一系列不同稀疏度的模型)。
    *   报告：`reports/edge_pruning_results.json`
*   **最终结果**：一系列经过剪枝和微调的稀疏模型，以及一份记录了它们目标稀疏度、实测稀疏度和评估准确率的报告。

### 4. 量化与评估 (Quantization & Evaluation)

*   **功能描述**：将训练好的浮点模型转换为适用于边缘部署的 TFLite 格式，并应用多种量化策略以实现极致压缩和加速。同时，在 CPU 上精确评测不同量化版本模型的**尺寸、延迟和准确率**。
*   **核心脚本**：<mcfile name="part3_convert_eval_legacy.py" path="MLS-A2/part3_convert_eval_legacy.py"></mcfile>
*   **实现函数**：
    *   `convert_all`：负责将 Keras 模型转换为 TFLite，并应用动态范围量化 (DRQ)、FP16 量化和 INT8 全整型量化。
    *   `eval_tflite`：负责加载 TFLite 模型并精确测量其在 CPU 上的单样本推理延迟和准确率。
*   **基础模型**：通过 `--weights` 参数传入的 `.h5` 权重文件，加载到 `build_student` 或 `build_depthwise` 模型中。
*   **产出文件**：
    *   模型：`edge_optimized_models/..._drq.tflite`, `..._fp16.tflite`, `..._int8.tflite`。
    *   报告：`reports/edge_quant_results_legacy.json`
*   **最终结果**：产出了可直接部署的 TFLite 模型，以及一份最终的量化性能对比报告，清晰地展示了不同量化策略在模型尺寸、推理速度和准确率之间的权衡 (Trade-off)，为选择最终部署方案提供了直接的数据支持。

*   **运行命令**：
```bash
python part3_convert_eval_legacy.py --arch depthwise \
  --width 0.75 --depth 1.0 \
  --weights edge_optimized_models/depthwise_w0_75_d1_0.weights.h5 \
  --int8
```    